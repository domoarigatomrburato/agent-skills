interface:
  display_name: "Inquisition"
  short_description: "Three fresh reviews and verified fixes for changes"
  default_prompt: "Use $inquisition to review my current changes for correctness, test evidence, and design, then fix supported issues."
policy:
  allow_implicit_invocation: false
