interface:
  display_name: "Inquisition"
  short_description: "Two fresh reviews and verified fixes for changes"
  default_prompt: "Use $inquisition with two fresh reviewers: one for correctness and test evidence, one for design and efficiency; then fix supported issues."
policy:
  allow_implicit_invocation: false
