interface:
  display_name: "Polish"
  short_description: "Polish changed code without changing behavior"
  default_prompt: "Use $polish to review and refine my current code changes without changing behavior."
policy:
  allow_implicit_invocation: false
